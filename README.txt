This is an encrypted steganography software. To hide text within an image, a password needs to be set. When extracting, entering the password will output the text as a TXT file. The underlying principle of steganography is LSB (Least Significant Bit).

encode  --input input_path --output outout_path --embed text_path

decode --input input_path  --passwd VAR